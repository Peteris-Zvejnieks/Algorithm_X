#add_library(pat INTERFACE)
#target_include_directories(pat INTERFACE  ${PROJECT_SOURCE_DIR}/src)

#target_link_libraries(pat INTERFACE range-v3)
#target_link_libraries(pat INTERFACE pybind11)
#target_link_libraries(pat INTERFACE cppcoro)